#include <cstring>
#include <cassert>
#include <vector>
#include <list>
#include <queue>
#include <map>
#include <set>
#include <deque>
#include <stack>
#include <bitset>
#include <algorithm>
#include <functional>
#include <numeric>
#include <utility>
#include <sstream>
#include <iostream>
#include <iomanip>
#include <cstdio>
#include <cmath>
#include <cstdlib>
#include <ctime>
#include <fstream>
#include <climits>

#define LL long long
#define MAX 10009
#define clr(aa, bb) memset(aa, bb, sizeof aa)
#define MOD 5000000
#define N 1000007
#define check_bit(a,b) (a & (1<<b))
#define set_bit(a,b) (a | (1<<b))
#define PB push_back
#define FOR(aa,nn) for(aa=0; aa<nn; aa++)
#define vi vector<int>
#define vll vector<long long>


using namespace std;


LL S[MAX];
int f[MAX];
LL arr[MAX];
int val[10009];



int main()
{
    //   FILE * fin, * fout, *fp;
//     fp=fopen("out.txt", "w");

    int i,j;
    int a,b,h,mn=0;
    int n, K;
    set<LL>::iterator it;

    memset(arr, 0, sizeof arr);

    cin>>n>>K;
    for(i = 0; i < n; i++)
        cin>>arr[i];

    set<LL>S(arr, arr + n);
    int sz = S.size();
    int cnt = 0;
    for(it = S.begin(); it != S.end(); i++)
    {
        val[(*it)] = (++cnt);
    }
    for(i = 0; i < n; i++)
        arr[i] = val[arr[i]];

    for(i = 0; i < n; i++)
    {
        dp[i] = query(1, 1, sz, 1, arr[i] - 1);
        insert(1, 1, sz, arr[i], )
    }




    return 0;
}

/*

7 3
5 5 5 5 5 5
1 3 4
4 5 2
6 3 1



*/
